<!--
::neup.documentation::logica-account-token-folder
::title Logica Account Token Folder

Account token helper folder.

::public

This folder owns NeupID token decoding, expiry checks, signature verification,
and bridge-backed token authentication used by `logica.account.auth`.

::public end

::end
-->
